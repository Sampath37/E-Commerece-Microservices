server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Sampath37/ecommerce-config-repo.git
          default-label: main
          clone-on-start: true
